{
  "name": "enterprise-ai-os",
  "private": true,
  "version": "0.1.0",
  "description": "Enterprise AI Operating System",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "dev:all": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "build": "npm run build -w packages/shared && npm run build -w packages/stores && npm run build -w packages/connectors && npm run build -w packages/agent-core && npm run build -w apps/api && npm run build -w apps/web",
    "build:api": "npm run build -w packages/shared && npm run build -w packages/stores && npm run build -w packages/connectors && npm run build -w packages/agent-core && npm run build -w apps/api",
    "start:api": "npm run start -w apps/api",
    "db:migrate": "node db/migrate.js",
    "db:seed-admin": "node db/seed-admin.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "typescript": "^5.4.5"
  }
}