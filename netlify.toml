# Nexora web (Next.js) on Netlify — monorepo root
[build]
  base = "."
  command = "npm ci && npm run build -w apps/web"
  publish = "apps/web/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"
