# Nexora web — package-level Netlify config
[build]
  command = "cd ../.. && npm ci && npm run build -w apps/web"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"
