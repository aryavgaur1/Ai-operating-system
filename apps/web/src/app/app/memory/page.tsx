import { ComingSoon } from '@/components/ComingSoon';

export default function MemoryPage() {
  return <ComingSoon title="Memory" body="Persistent workspace and conversation memory will live here." />;
}
