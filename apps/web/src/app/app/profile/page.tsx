import { ComingSoon } from '@/components/ComingSoon';

export default function ProfilePage() {
  return <ComingSoon title="Profile" body="Profile settings are available under Settings for now." />;
}
